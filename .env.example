# Central API base URL
# - Direct API:  http://<api-host>:4000
# - Behind proxy: https://<monitoring-host>/api
CENTRAL_API_URL=https://monitoring.edge.ge/api

# Name shown in dashboard (can be any string)
SERVER_NAME=my-edge-server

# Agent key generated by the central API (shown once)
AGENT_API_KEY=replace-me

# How often to report
REPORT_INTERVAL_SECONDS=30

# Docker socket (Linux default)
DOCKER_SOCKET_PATH=/var/run/docker.sock
